テスト項目 (Test Specifications)※表示が崩れています。

本システムの品質を担保するためのテストケースを定義する。バックエンドAPIのテストには pytest および TestClient (FastAPI) を使用する。

1. ユニットテスト (Unit Test) - ビジネスロジック

主に SummaryService におけるスコア計算・ソートアルゴリズムを検証する。AtCoder的なコーナーケースを想定する。

テストID

テスト対象

条件・入力値

期待される結果

UT-001

スコア計算

◯(2)が2人、△(1)が1人、✕(0)が1人

その日程のスコアは 5 になること。

UT-002

ソート順位

日程A(スコア5, ◯2人), 日程B(スコア5, ◯1人)

日程Aが上位にソートされること（◯の人数を優先）。

UT-003

ソート順位(同値)

日程A(スコア4, ◯2, 3/14), 日程B(スコア4, ◯2, 3/15)

日付が早い日程Aが上位にソートされること。

UT-004

異常値排除

未回答のユーザーデータが混入

スコア計算時にエラーにならず、0点として無視されること。

2. 結合テスト (Integration Test) - APIエンドポイント

APIエンドポイントが正しくルーティングされ、DBのCRUD操作が仕様通りに行われるかを検証する。

2.1 回答登録API (POST /api/v1/answers)

テストID

観点

条件・リクエスト内容

期待されるHTTPステータス / 挙動

IT-A01

正常系

必須項目を全て満たす新規回答（ヘッダーに新規UUID）

200 OK / DBにUserとAnswerレコードが新規作成される。

IT-A02

正常系

既存UUIDによる回答の更新（ステータス変更）

200 OK / DBの既存AnswerレコードがUPDATEされる（重複登録されない）。

IT-A03

異常系(境界値)

status に未定義の値 (例: 3) を送信

422 Unprocessable Entity / バリデーションエラーが返る。

IT-A04

異常系(必須欠如)

username を空文字で送信

422 Unprocessable Entity / バリデーションエラーが返る。

IT-A05

異常系(不正ID)

存在しない event_date_id を送信

404 Not Found または DBの外部キー制約エラーを適切にハンドリングした 400 Bad Request。

2.2 一覧取得API (GET /api/v1/events/{event_id}/summary)

テストID

観点

条件・リクエスト内容

期待されるHTTPステータス / 挙動

IT-S01

正常系

回答データが存在するイベントIDを指定

200 OK / レコメンド配列とマトリクス配列が正しいJSON形式で返る。

IT-S02

正常系(空)

回答者が0人のイベントIDを指定

200 OK / レコメンドスコアが全て0、マトリクスが空配列で返る（エラーにならないこと）。

IT-S03

異常系

存在しない event_id を指定

404 Not Found / 「イベントが存在しません」のメッセージ。
